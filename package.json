{
  "name": "halbimetal-platform",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --configLoader runner",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "^7.1.0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.4"
  }
}
